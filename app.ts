// select dom element
const form = document.getElementById("todo-form") as HTMLElement
const input = document.getElementById("todo-input") as HTMLElement
const todoList = document.getElementById("todo-list") as HTMLElement

// event

